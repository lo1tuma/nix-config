interface:
  display_name: "Simplify Codebase"
  short_description: "Remove needless custom code"
  default_prompt: "Use $simplify-codebase to deeply analyze this repository and create focused PRs for the highest-impact simplifications."

policy:
  allow_implicit_invocation: true
