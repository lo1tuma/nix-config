interface:
  display_name: "Simplify Codebase"
  short_description: "Remove needless custom code"
  default_prompt: "Use $simplify-codebase to remove unnecessary hand-rolled code and deepen shallow modules."

policy:
  allow_implicit_invocation: true
